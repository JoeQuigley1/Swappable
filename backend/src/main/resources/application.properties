spring.application.name=backend
server.port=${PORT:8080}

# Database (Using localhost instead of host.docker.internal since you are running locally)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/swappable}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true


# JWT
jwt.secret=3c9c34a2a51d428bda4c5f822a16d594b2a514d82bfa8c716e251b1a43a2
jwt.expiration=86400000

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always

# Email
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=aed687001@smtp-brevo.vom
spring.mail.password=xsmtpsib-6e24dd083cf14c3c278049adf30bda9419255b7e4d72fc82280852fd495781c5-6RPWjPLLg2cIErdq?
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}
mail.from=swappable2026@gmail.com
app.mail.from=noreply@swappable.com

# Image upload limits
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=50MB